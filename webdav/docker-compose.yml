version: '3.8'

services:
  webdav:
    image: bytemark/webdav
    container_name: webdav
    environment:
      - AUTH_TYPE=Basic
       - USERNAME=${USERNAME}
       - PASSWORD=${PASSWORD}
    ports:
      - "8086:80"
    volumes:
      - ./data:/var/lib/dav
